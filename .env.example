# Copy this file to `.env` and replace the placeholder values.
# The application loads these variables via python-dotenv.

# Gemini API keys are now loaded from `config/gemini.env`.
# See `config/gemini.env.example` for a template.

# Flask secret key for session signing (optional override)
# FLASK_SECRET_KEY=change-me
